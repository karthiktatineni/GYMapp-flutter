name: gym_app
description: A premium workout planner and suggester app.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.11.0
  firebase_auth: ^5.4.3
  cloud_firestore: ^5.6.1
  provider: ^6.1.1
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.6.0
  intl: ^0.19.0
  fl_chart: ^0.66.0
  percent_indicator: ^4.2.3
  cached_network_image: ^3.3.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  google_generative_ai: ^0.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

