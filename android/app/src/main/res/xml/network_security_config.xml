<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Network Security Configuration for enhanced security.
    This file forces HTTPS connections and prevents man-in-the-middle attacks.
-->
<network-security-config>
    <!-- Disable cleartext (HTTP) traffic globally -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust only system CA certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow debug builds to use localhost for development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- Domain-specific configuration for Firebase and Google services -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">firebasestorage.app</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
